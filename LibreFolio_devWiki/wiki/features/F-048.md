---
code: F-048
title: "Transaction Modals — Form / Bulk / Delete / Promote / Split (mode-less, Round 6 Plan D done)"
layer: fullstack
status: in-progress
mkdocs: null
tags: [transactions, frontend, backend, modal, formModal, bulkModal, deleteModal, promoteWizard, validate-scheduler, type-rules, dual-form, i18n-errors, bulk-delete, mode-less, context-menu, broker-access, cash-transfer, tagInput, txStore, pendingOp, pair-validation, contextual-validate, split, promote, promote-suggest, promote-merge-modal, pmc-auto-calc]
depends_on: [F-046, F-047]
enables: [F-049]
related_decisions:
  - decisions/multi-broker-atomic-tx
  - decisions/unified-batch-pipeline
  - decisions/server-driven-type-rules
  - decisions/dual-transaction-form-design
  - decisions/bulkmodal-mode-removal
  - decisions/cash-transfer-split-promote
  - decisions/context-menu-all-tables
  - decisions/broker-access-min-paired
  - decisions/txstore-single-source-of-truth
  - decisions/pendingop-tagged-union
  - decisions/pair-description-tags-validation
  - decisions/formmodal-contextual-validate
  - decisions/end-of-day-balance-check
related_problems:
  - problems/svelte5-effect-read-write-loop
  - problems/browser-autofill-numeric-fields
  - problems/dual-form-collect-duplication
  - problems/wac-feedback-loop
  - problems/clone-link-uuid-duplication
related_concepts:
  - concepts/validate-scheduler-pattern
  - concepts/resolve-validation-message-pattern
  - concepts/savewithretry-frontend-pattern
  - concepts/entity-store-pattern
  - concepts/txstore-pattern
  - concepts/paired-partner-architecture
  - concepts/stateless-preview-pattern
---

# F-048 — Transaction Modals (Form / Bulk / Delete / Promote / Split)

## What it does
Five specialised modal components for creating, editing, duplicating, deleting, splitting, and promoting transactions. Replace the original monolithic `TransactionStagingModal`. All validate via `POST /transactions/validate` and commit via `POST /transactions/commit` using the unified batch pipeline (including `splits[]` and `promotes[]`).

## Layer breakdown
**Backend**: Pipeline Steps 5b (splits) and 5c (promotes) in `execute_batch`; `POST /transactions/promote-suggest` for DB candidate matching; PMC auto-calc (`compute_weighted_avg_cost`) on TRANSFER receiver; `consumed_link_uuids` preventing Step 6 re-processing.

**Frontend**: Five components in `frontend/src/lib/components/transactions/`:
- `TransactionFormModal.svelte` — single-row CRUD (create/edit/duplicate/view) + dual-form mode for paired transactions
- `TransactionBulkModal.svelte` — **Mode-less** DataTable-based unified batch editor: create + edit + delete + split + promote in one modal. See [[decisions/bulkmodal-mode-removal]]
- `TransactionDeleteModal.svelte` — Rich single/paired delete confirmation with 3 layouts (A/B/C), server-side validation, error resolution
- `PromoteMergeModal.svelte` — 3-column diff UI for resolving divergent fields between promote candidates (description, tags, date, cost_basis_override)
- `PromotePairWizardModal.svelte` — 3-step wizard for promoting DEPOSIT/WITHDRAWAL pairs (legacy, being superseded by PromoteMergeModal flow)

## Architecture (Phase 7 Part 4 Rounds 3–6)

### TransactionFormModal
- **Modes**: create, edit, duplicate, view
- **Dual-form mode** (R6-B): `pair_form_layout` from `transactionTypeStore` drives 3 layouts: FX (split currencies), Transfer Asset (split brokers, shared asset), Transfer Cash (split brokers, shared amount). See [[decisions/dual-transaction-form-design]]
- **Sectioned form**: Required (Date, Type, Qty, Cash, Asset, Broker) → Optional (tags, description) → Advanced (event link, cost_basis_override, pair partner)
- **Type-driven field adaptation**: all fields adapt visibility/labels/sign hints from server-driven type rules ([[decisions/server-driven-type-rules]])
- **Auto-sign negation**: user enters positive numbers when type requires negative (SELL qty, BUY cash) — auto-negated in `collectCreate()`/`collectUpdate()`
- **Unsaved changes guard**: JSON snapshot comparison triggers ConfirmModal on close/escape
- **Tags autocomplete**: client-side aggregation from loaded transactions + drafts
- **PATCHABLE_FIELDS allowlist** (Bugfix 3): update payloads strip immutable fields via shared `txPayloadHelpers.ts` allowlist
- **Type swap** (Bugfix 3): backend supports type change within swap groups (BUY↔SELL, DEPOSIT↔WITHDRAWAL, DIVIDEND↔INTEREST, TAX↔FEE). Paired types locked. Sign-flip on quantity/cash automatic.
- **Shared payload helpers** (Plan A): `txPayloadHelpers.ts` — single source for PATCHABLE_FIELDS, applySignRules, buildUpdateDiff, fieldEq. Eliminates FormModal/BulkModal logic duplication (see [[problems/dual-form-collect-duplication]])
- **Broker access** (Plan B): form dropdown shows only `getEditableBrokers()` (OWNER/EDITOR). View mode for VIEWER/inaccessible partners shows locked placeholder. `canEdit` prop gates ✏ button.
- **Mismatched description handling** (Plan C2): when paired edit detects divergent descriptions between sides, concatenates with `[auto-merged: pair had mismatched descriptions]` note — makes change explicit to user
- **Context-aware validate** (Plan C2R2): FormModal sends entire bulk context to `/validate` via `getBulkContext()` callback from BulkModal. Issues filtered to current row only. Contextual i18n banner header for inter-dependency errors. See [[decisions/formmodal-contextual-validate]]

### TransactionBulkModal (mode-less unified batch editor)
- **Mode-less design** (Plan B23 follow-up): no `mode` prop. Each row's role inferred from its data — `tx.id > 0` = DB row (edit), `tx.id ≤ 0` = new draft (create). See [[decisions/bulkmodal-mode-removal]]
- **txStore refactor** (Plan C): BulkModal no longer holds full row copies. Reads from `txStore.get(id)` and holds only `PendingOp[]` (create/edit/delete instructions). Status derived automatically from diff vs txStore. See [[concepts/txstore-pattern]], [[decisions/txstore-single-source-of-truth]]
- **PendingOp tagged union** (Plan C3): `DraftRow` eliminated → replaced by `PendingOp` discriminated union (`op: 'create' | 'edit'`) + `DraftFields` (pure data) + `PartnerDisplay` (partner rendering). `d.original` eliminated — single source of truth is always `txStoreGet(op.txId)`. Status always derived via `deriveStatus(op)`. `drafts` renamed to `ops`. See [[decisions/pendingop-tagged-union]]
- **Factory functions** (Plan C3): `fromTx()` eliminated → `fieldsFromTx()` (pure extraction) + `editOpFromTx()` + `createOpFromClone()`. Type tightened: `partnerPayload: TxFields | null` (was `Record<string,unknown>`)
- Built on `DataTable` with editable cells
- **Row states**: `new` (green CREATE bg-emerald-50/40), `original` (unchanged, skipped), `edited` (amber UPDATE bg-amber-50/40), `delete` (red DELETE with line-through)
- **Row background tints** (Appendix 1 Fix F): `getRowClass()` applies status-specific background colors in both light and dark mode
- **Bulk delete flow**: opened with `initialStatus: 'delete'` — all rows start marked for deletion, user can restore/edit/clone/add from the same batch
- **Confirm edit on delete**: editing a row marked for deletion triggers a ConfirmModal ("Restore & Edit?") before opening FormModal
- **Paired row split hint**: InfoBanner shown when paired rows are marked for deletion, suggesting Split to unlink before partial delete
- **PickerModal**: reads from `txStore.getAll()` directly — zero props. Guard for non-editable rows (VIEWER broker) via `txStore.canEdit()` with disabled checkbox (⊘), tooltip, select-all skip. Double-click (desktop) and long-press (mobile) toggle selection
- **Auto-validate**: disabled when N > 50 rows; manual `⚡ Validate now` always available. See [[concepts/validate-scheduler-pattern]]
- **i18n error display**: structured error codes resolved via `resolveValidationMessage.ts`. See [[concepts/resolve-validation-message-pattern]]
- **Anti-bounce**: 10s deduplication on validate+commit when draftKey unchanged
- **Commit failure banner**: red ⛔ banner with categorised issues (field errors vs balance errors with client-side row attribution)
- **CASH_TRANSFER** (Bugfix 1): `collectDualCreates('transfer_cash')` emits CASH_TRANSFER+CASH_TRANSFER (not WITHDRAWAL+DEPOSIT). See [[decisions/cash-transfer-split-promote]]
- **Paired edit fix** (R7-C1, Plan A): `patchDualRowFromForm()` preserves partner id/status/original for DB rows; `collectDualCreates()` reuses existing `link_uuid` in edit mode
- **Responsive footer** (Appendix 1 Step 3): `<span class="hidden sm:inline">` pattern on all footer buttons — icon-only on mobile with `title` attribute
- **Conditional "Reset all"** (Appendix 1 Step 6): only visible when rows have `edited`/`delete` status with `original` baseline
- **"Reset selected"** (Appendix 1 Fix C): inline toolbar button resets only selected rows
- **Viewer-only guard** (Appendix 1 Step 4): `filterEditableRows()` filters bulk selections before opening modal; toast warning for skipped, error if all read-only

### TransactionDeleteModal (3-layout rich confirmation)
- **Layout A** (standalone): full detail table — Date, Type (with icon), Qty, Cash, Asset (with icon), Broker (BrokerBadge + role), Tags (colored), Description (with tooltip)
- **Layout B** (paired full): side-by-side From/To table with giver/receiver columns, asset icons, broker badges. Split hint below. `maxWidth="xl"` (Appendix 1 Step 2)
- **Layout C** (paired blocked/inaccessible): partner broker locked — shows Lock icon, broker name, "not accessible" message. Delete button hidden
- **Validation**: `errors[]` prop for backend error display via `resolveIssueMessage()`, `validating`/`validated` props, `onValidate` callback for "⚡ Validate now" button
- **Structured success toast** (Appendix 1 Step 1): HTML with transaction type icon, broker icon, preposition, date. Paired variant shows from→to with broker icons
- **Broker access** (Plan B): Layout C shown when user lacks EDITOR on partner broker. Split hint in Layout B. See [[decisions/broker-access-min-paired]]

### PromoteMergeModal (3-column diff — Plan D)
- **Purpose**: resolves divergent fields when promoting two transactions into a paired type
- **When shown**: Main Table promote or BulkModal promote toolbar when description/tags/date/cost_basis_override differ between the two rows
- **Layout**: 3-column per divergent field — left (readonly, txA value), center (editable, result), right (readonly, txB value)
- **Auto-populate buttons**: `◀` (use left), `⟷` (merge — concat desc, union tags, latest date), `▸` (use right)
- **Global buttons**: "All Left ▸", "⟷ Merge All", "◀ All Right" at bottom
- **Pre-populate default**: merge (concatenated description, union-deduplicated tags, latest date, txA cost basis)
- **Tags**: uses `TagInput.svelte` for chip input with autocomplete
- **Textarea**: auto-grow via `field-sizing: content; max-height: 8rem`
- **Skip**: if all 4 fields are identical between txA and txB, modal is skipped — `onConfirm({})` called directly
- **Output**: `resolved_fields` dict sent in `promotes[].resolved_fields` — backend applies to both TX sides

### Split/Promote in BulkModal (Plan D)
- **Split row action** (`✂ Split`): icon `Unlink` from lucide-svelte
  - **Saved paired** (`op='edit'`, `partnerId != null`): accumulates `{id_a, id_b}` in `pendingSplits[]; does NOT mutate `fields.type`; shows badge purple "✂ split"; column type shows "TYPE → ✂ TARGET"; partner becomes visible as separate edit op; commit payload includes `splits[]` and skips these ops from updates
  - **New paired** (`op='create'`, shared `link_uuid`): local transformation — remove `link_uuid` from both, mutate types per client-side `SPLIT_TYPE_MAP`, separate into 2 independent ops. No API call.
  - **Undo split**: removes from `pendingSplits`, removes partner op, restores paired display
- **Promote toolbar**: visible when 2 standalone rows selected + `findPromoteMatch()` valid
  - **2 saved** → PromoteMergeModal → `pendingPromotes[{id_a, id_b, resolved_fields}]` in batch
  - **2 new** → local transform: assign shared `link_uuid`, mutate types to target. No promotes[] in batch (goes as 2 creates paired via Step 6)
  - **1 saved + 1 new** → `pendingPromotes[{id_a, link_uuid_b, resolved_fields}]`
- **Promote-suggest banner** (green, above grid):
  - **DB candidates**: `$effect` debounce 500ms → `POST /transactions/promote-suggest` for edit standalone ops
  - **Local candidates**: `$derived` pure `findPromoteMatch()` on new standalone ops
  - **Delta-days slider**: range 0-14, `accent-libre-green`, persists in session
  - Click 🔗 → auto-select rows + open PromoteMergeModal
  - `data-testid="promote-suggest-banner"`, `data-testid="promote-suggest-item-{idx}"`

### Split/Promote in Main Table (Plan D)
- **Split row action**: `[data-action-id="split"]` on paired rows; ConfirmModal (TransactionActionModal) with tabular detail; commits via `{splits: [{id_a, id_b}]}` through batch
- **Promote toolbar**: 2-row selection + type match → if fields diverge opens PromoteMergeModal, else direct ConfirmModal; commits via `{promotes: [{id_a, id_b, resolved_fields}]}` through batch (migrated from eliminated `/promote` endpoint)

### TagInput.svelte (Bugfix 3 + Round 6)
- New chip input component with autocomplete dropdown from available tags
- Keyboard navigation: ArrowDown/Up + Enter on highlight
- Colored chips via `getStringColor()` / `getStringBadgeStyle()`
- Anti-bounce via `relatedTarget` check (replaces fragile setTimeout)
- Chip navigation: ←→ + Delete/Backspace on selected chip with `ring-2` highlight

### PMC Auto-Calc (Plan D, Bugfix 4)
- **Backend**: `compute_weighted_avg_cost(session, broker_id, asset_id, as_of_date)` — weighted average of BUY prices + incoming TRANSFER cost_basis at source broker
- Applied automatically to TRANSFER receiver (qty>0) when `cost_basis_override` is null at commit
- On promote to TRANSFER: auto-calc if receiver has no override
- `cost_basis_override` applied ONLY to receiver (qty>0); sender (qty<0) forced to null
- FormModal shows warning banner for ADJUSTMENT qty>0 without cost_basis_override

## Status history
| Date | Status | Notes |
|------|--------|-------|
| 2026 | planned | Phase 7 Part 5 |
| 2026-04-28 | in-progress | Manual mode (create-many, edit-many) built in Part 4 |
| 2026-04-28 | in-progress | Round 3: three-component rewrite (FormModal, BulkModal, PromoteWizard) |
| 2026-04-29 | in-progress | Round 3 Bugfix 1-2: FormModal redesign, i18n validation errors |
| 2026-04-29 | in-progress | Round 4: unified batch pipeline, error display unification |
| 2026-04-30 | in-progress | Round 5: server-driven type rules, auto-sign, dual-form (R6-B) |
| 2026-04-30 | in-progress | Round 5 Bugfix 1: CASH_TRANSFER enum, split/promote architecture |
| 2026-05-02 | in-progress | Round 5 Bugfix 2: BulkModal readonly, "✓ Applica", dual dates |
| 2026-05-03 | in-progress | Round 5 Bugfix 3: PATCHABLE_FIELDS, type swap, TagInput, SafeDecimal |
| 2026-05-05 | in-progress | Round 6 Plan A: ContextMenu, R7-C1/H1 fix, txPayloadHelpers.ts |
| 2026-05-05 | in-progress | Round 6 Plan B: Broker access, DeleteModal 3 layouts, PickerModal |
| 2026-05-06 | in-progress | Round 6 Plan B1: 16 bugfixes, 48+ E2E tests, partner_broker_id |
| 2026-05-07 | in-progress | Round 6 Plan B23: Mode-less BulkModal, bulk delete flow |
| 2026-05-08 | in-progress | Round 6 Plan B23 Appendix 1: UI polish (toast, responsive, row tints) |
| 2026-05-08 | in-progress | Round 6 Plan C: txStore refactor — single source of truth, -30% LOC |
| 2026-05-10 | in-progress | Round 6 Plan C2: 6 bugfixes, pair desc/tags validation, 68→80 E2E |
| 2026-05-11 | in-progress | Round 6 Plan C2R2: MockFX, auto-populate removal, contextual validate |
| 2026-05-11 | in-progress | Round 6 Plan C3: PendingOp tagged union, drafts→ops rename |
| 2026-05-12 | in-progress | Round 6 Plan D1: Backend batch split/promote, eliminate /split /promote endpoints, promote-suggest, 18 tests |
| 2026-05-12 | in-progress | Round 6 Plan D2: PromoteMergeModal, BulkModal split/promote actions, Main Table promote migration, suggest banner |
| 2026-05-12 | in-progress | Round 6 Plan D2-bf1: 21 bugs, getTypeRule race fix, findPromoteMatch constraints, collapse post-promote |
| 2026-05-13 | in-progress | Round 6 Plan D2-bf2: Split preview editability (corrects DD-BF1), pipeline reorder (splits before updates), ids[] schema |
| 2026-05-14 | in-progress | Round 6 Plan D2-bf3: Split schema {id_a,id_b}, suggest banner redesign, E2E (absorbed D3), TransactionActionModal unification |
| 2026-05-14 | in-progress | Round 6 Plan D2-bf4: PMC auto-calc, cost_basis only on receiver, suggest constraint fix, link_uuid sync, delta-days slider |
| 2026-05-24 | in-progress | R2 SP-A: cost_basis_override → Currency object, WAC service with FX, recalc-wac endpoint |
| 2026-05-24 | in-progress | R2 SP-B: 13 WAC tests + financial-utils tests |
| 2026-05-30 | in-progress | R2 SP-C: BulkModal suggest overhaul, PromoteMergeModal simplification, split UX improvements |
| 2026-05-24 | in-progress | R2 SP-C BugfixRound1: 12 bugs fixed (commit logic, suggest redesign, UX) |
| 2026-05-26 | in-progress | R2 SP-C BugfixRound2: WAC Preview + Unified Partner Architecture |
| 2026-05-30 | in-progress | R2 SP-C WacPreview chain: 8 plans (reactive WAC, feedback loop, clone fix, stateless preview) |

## Deferred / open items
- `create-brim` mode: BRIM import output → staging (Part 5)
- E2E specs: `transactions-form`, `-bulk` (deferred; `-split-promote` done)
- R2 SP-D: FormModal CompactCash, AssetEvent picker, WAC Info modal, store-first (⏳ next)
- R2 SP-E: E2E tests for WAC/suggest/picker features (🔲 TODO)

## Source files

| Role | Path |
|------|------|
| Form modal | `frontend/src/lib/components/transactions/TransactionFormModal.svelte` |
| Bulk modal (mode-less) | `frontend/src/lib/components/transactions/TransactionBulkModal.svelte` |
| Delete modal (3-layout) | `frontend/src/lib/components/transactions/TransactionDeleteModal.svelte` |
| Promote merge modal | `frontend/src/lib/components/transactions/PromoteMergeModal.svelte` |
| Promote wizard (legacy) | `frontend/src/lib/components/transactions/PromotePairWizardModal.svelte` |
| Transaction action modal | `frontend/src/lib/components/transactions/TransactionActionModal.svelte` |
| Picker modal | `frontend/src/lib/components/transactions/TransactionPickerModal.svelte` |
| txStore (single source of truth) | `frontend/src/lib/stores/txStore.svelte.ts` |
| TX interface types (canonical) | `frontend/src/lib/components/transactions/types.ts` |
| Context menu | `frontend/src/lib/components/ui/ContextMenu.svelte` |
| Tag input | `frontend/src/lib/components/ui/TagInput.svelte` |
| TX payload helpers | `frontend/src/lib/utils/txPayloadHelpers.ts` |
| Broker role helpers | `frontend/src/lib/utils/brokerRoleHelpers.ts` |
| Compact cash cell | `frontend/src/lib/components/ui/CompactCashCell.svelte` |
| Type search select | `frontend/src/lib/components/transactions/TransactionTypeSearchSelect.svelte` |
| Validate scheduler | `frontend/src/lib/utils/useValidateScheduler.svelte.ts` |
| Validation resolver | `frontend/src/lib/utils/resolveValidationMessage.ts` |
| Decimal formatting | `frontend/src/lib/utils/formatDecimal.ts` |
| Transaction type store | `frontend/src/lib/stores/transactionTypeStore.ts` |
| Transaction API | `backend/app/api/v1/transactions.py` |
| Transaction service | `backend/app/services/transaction_service.py` |
| Transaction schemas | `backend/app/schemas/transactions.py` |
| Balance walk tests | `backend/test_scripts/test_api/test_tx_balance_walk.py` |
| Batch split/promote tests | `backend/test_scripts/test_api/test_transactions_batch_split_promote.py` |
| E2E split/promote | `frontend/e2e/transactions/tx-split-promote.spec.ts` |
| E2E broker access | `frontend/e2e/transactions/tx-broker-access.spec.ts` |
| E2E paired edit | `frontend/e2e/transactions/tx-paired-edit.spec.ts` |
| E2E tooltips | `frontend/e2e/transactions/tx-tooltips.spec.ts` |
| E2E delete | `frontend/e2e/transactions/tx-delete.spec.ts` |
| E2E picker pagination | `frontend/e2e/transactions/tx-picker-pagination.spec.ts` |
| E2E bulk operations | `frontend/e2e/transactions/tx-bulk-operations.spec.ts` |
| E2E clone | `frontend/e2e/transactions/tx-clone.spec.ts` |
| Mock data (promote-test) | `backend/test_scripts/test_db/populate_mock_data.py` |
