# Secrets
.env

# Temporary Aphra config (generated by translate script, contains API key)
config.toml

# Translation cache (local state, not committed) - commited to enable other devices to update doc translation and avoid re-translating non changing files
# .translate-hashes.json

# Python
__pycache__/
*.pyc

