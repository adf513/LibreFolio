{
	"name": "librefolio",
	"version": "0.5.x",
	"description": "LibreFolio - Self-hosted financial portfolio tracker",
	"private": true,
	"scripts": {
		"install:all": "npm install && cd frontend && npm install",
		"test:e2e": "playwright test"
	},
	"devDependencies": {
		"@playwright/test": "^1.48.2",
		"prettier": "^3.8.1"
	}
}
